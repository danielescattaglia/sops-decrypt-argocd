apiVersion: v1
kind: Service
metadata:
  name: sops-decrypt-service
  namespace: argocd-plugins
spec:
  selector:
    app.kubernetes.io/name: sops-decrypt
  ports:
  - name: http
    port: 80
    targetPort: 4355
